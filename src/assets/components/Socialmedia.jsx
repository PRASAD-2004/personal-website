import { useState } from 'react'

export default function Scoial(props){
    
    return (

        <div>
            <a href= {props.href} ><img src={props.src} className='media'/></a>

        </div>
    )}